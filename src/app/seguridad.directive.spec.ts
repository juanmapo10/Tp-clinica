import { SeguridadDirective } from './seguridad.directive';

describe('SeguridadDirective', () => {
  it('should create an instance', () => {
    const directive = new SeguridadDirective();
    expect(directive).toBeTruthy();
  });
});
