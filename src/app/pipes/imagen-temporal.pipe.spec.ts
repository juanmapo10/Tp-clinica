import { ImagenTemporalPipe } from './imagen-temporal.pipe';

describe('ImagenTemporalPipe', () => {
  it('create an instance', () => {
    const pipe = new ImagenTemporalPipe();
    expect(pipe).toBeTruthy();
  });
});
